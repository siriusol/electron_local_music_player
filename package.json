{
  "name": "electron_local_music_player",
  "version": "0.0.1",
  "description": "A music player that support play local music.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch main.js --exec electron ."
  },
  "keywords": [
    "electron"
  ],
  "author": "Ther",
  "license": "MIT",
  "devDependencies": {
    "electron": "^4.1.3",
    "nodemon": "^2.0.15"
  }
}
